{
  "name": "startup-job-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Local job search agent for Anmol More — finds real India startup openings via Claude + web search",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
